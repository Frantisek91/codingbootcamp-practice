
let account1 = "000256784";
let account2 = "000986056";
let account3 = "001546238";
let account4 = "030987456";

let amount1 = 790;
let amount2 = 5600;
let amount3 = 78065;
let amount4 = 120910;

let yourAccount = prompt ("Please enter your account number.");

let userBalance = null;


if (yourAccount === account1) {
    userBalance = amount1;
} else if (yourAccount === account2) {
    userBalance = amount1;
} else if (yourAccount === account3) {
    userBalance = amount1;
} else if (yourAccount === account4) {
    userBalance = amount1;
}

if (userBalance===null) {
    aler ("Incorrect account number")
} else (userBalance)

/* 
const square = (a) => {
    return a*a
}

const distance (a, b) => {
    if (a > b) {
        return a - b;
    } else {
        return b - a;
    }
}

const secret = (c) => {
    return distance (c, c*10)
}

const apply (fn, a) => {
    return fn(a);
}

console.log(secrec(5))
*/

